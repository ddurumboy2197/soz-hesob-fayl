def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for i, qator in enumerate(file1):
            if i % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz('input.txt', 'output.txt')
```

```python
def juft_qatorlarni_yoz(fayl1, fayl2):
    with open(fayl1, 'r') as file1, open(fayl2, 'w') as file2:
        for qator in file1:
            if file1.tell() % 2 == 0:
                file2.write(qator)

juft_qatorlarni_yoz
